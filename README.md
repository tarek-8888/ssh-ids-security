# ssh-ids-security
use this script to prevint Ddous attack on linux os 
use sudo apt update first
the use bash script command "bash neo2-log.sh"
make sure your add on shell "chmod +x neo2-log.sh" before start bash command.
